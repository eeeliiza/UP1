<Window
    x:Class="UP.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UP"
        mc:Ignorable="d"
        Title="Список студентов" Height="500" Width="850"
        WindowStartupLocation="CenterScreen"
        Background="#FFB3E5FC">
  

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        

        
        <StackPanel Orientation="Horizontal" Margin="0,0,0,15" HorizontalAlignment="Center">

            <TextBox x:Name="NameTextBox" Width="200" Height="30"
                     VerticalContentAlignment="Center"
                     FontSize="14"
                     Padding="5"
                     BorderThickness="0"
                     Background="#FFE0F7FA"
                     Foreground="#FF004D40"/>

            <TextBox x:Name="AgeTextBox" Width="80" Margin="5" Height="30"
                     VerticalContentAlignment="Center"
                    
                     FontSize="14"
                     Padding="5"
                     BorderThickness="0"
                     Background="#FFE0F7FA"
                     Foreground="#FF004D40"/>

            <TextBox x:Name="GroupTextBox" Width="120" Margin="5" Height="30"
                     VerticalContentAlignment="Center"
                     
                     FontSize="14"
                     Padding="5"
                     BorderThickness="0"
                     Background="#FFE0F7FA"
                     Foreground="#FF004D40"/>

            <Button Content="Добавить" Width="120" Height="30" Margin="5"
                    Click="Add_Click"
                    FontWeight="Bold" Foreground="White"
                    Cursor="Hand"
                    Background="#FF00BCD4">
                <Button.Style>
                    <Style TargetType="{x:Type Button}">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type Button}">
                                    <Border CornerRadius="10" Background="{TemplateBinding Background}">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>


        <DataGrid x:Name="StudentsGrid" Grid.Row="1"
                  AutoGenerateColumns="False" IsReadOnly="True"
                  CanUserAddRows="False" Margin="0,0,0,15"
                  FontSize="14"
                  Background="#FFE0FFFF"
                  
                  GridLinesVisibility="None"
                  BorderThickness="0"
                  Foreground="#FF004D40">
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsChanged}" Value="True">
                            <Setter Property ="Background" Value ="#80FF0000"/>
                            <Setter Property ="Foreground" Value ="White"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>

            <DataGrid.Columns>
                <DataGridTextColumn Header="ФИО" Binding="{Binding FullName}" Width="*" />
                <DataGridTextColumn Header="Возраст" Binding="{Binding Age}" Width="100"/>
                <DataGridTextColumn Header="Группа" Binding="{Binding GroupName}" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>

        
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="Редактировать" Width="120" Height="35" Margin="10,0"
         Click="Edit_Click"
         FontWeight="Bold" Foreground="White" Cursor="Hand"
         Background="#FF0097A7">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border CornerRadius="12" Background="{TemplateBinding Background}">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
            </Button>
            <Button Content="Удалить" Width="120" Height="35" Margin="10,0"
                    Click="Delete_Click"
                    FontWeight="Bold" Foreground="White" Cursor="Hand"
                    Background="#FF0097A7">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border CornerRadius="12" Background="{TemplateBinding Background}">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
            </Button>

            <Button Content="Сохранить" Width="120" Height="35" Margin="10,0"
                    Click="Save_Click"
                    FontWeight="Bold" Foreground="White" Cursor="Hand"
                    Background="#FF0097A7">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border CornerRadius="12" Background="{TemplateBinding Background}">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>

        
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                <GradientStop Color="#FFB3E5FC" Offset="0"/>
                <GradientStop Color="#FF80DEEA" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>

    </Grid>
</Window> 





    
    
